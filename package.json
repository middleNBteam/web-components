{
  "name": "webcomponents",
  "version": "0.0.2",
  "descriptions": "",
  "scripts": {
    "prebuild": "tsc -p ./worker/tsconfig.json && tsc",
    "predev": "tsc -p ./worker/tsconfig.json && tsc",
    "test": "echo \"Error: noe test specified\" && exit 1",
    "dev": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
